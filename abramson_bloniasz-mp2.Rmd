---
title: "Mini Project 2"
author: "Mathena Abramson"
date: "October 23, 2017"
output: html_document
---

```{r, message = FALSE}
library(tidyverse)
load("house_elections.rda")
load("candidates.rda")
load("committees.rda")
load("contributions.rda")
```

```{r}
glimpse(house_elections)
glimpse(candidates)
glimpse(committees)
glimpse(contributions)
```

```{r}
cand12 <- candidates %>%
  filter(cand_office == "H")
  
cand_contrib <- cand12 %>%
  inner_join(contributions, by = "cand_id")

glimpse(cand_contrib)
```

```{r}
sel_cand_cont <- cand_contrib %>%
  select(1:8, -7, 16, 19, 21:23, 27:30, 35)

glimpse(sel_cand_cont)
```

```{r}
full_contrib <- sel_cand_cont %>%
  inner_join(committees, by = "cmte_id")

glimpse(full_contrib)
```
